import React, {FunctionComponent} from 'react';

import spinner from './spinner.gif';

const Spinner: FunctionComponent = () => <img src={spinner} alt="loader" width="60" height="60" />;

export default Spinner;
